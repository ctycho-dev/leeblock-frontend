import { FC } from "react";

interface ICatalog { }

const Catalog: FC<ICatalog> = ({ }) => {

    return (
        <>
            <h1 className="text-red">Catalog</h1>
        </>
    )
}

export default Catalog